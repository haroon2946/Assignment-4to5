interface HeaderItems {
    lable: string;
    page: string;
}

export type NavItems = {
    home: { label: string, page: string };
    about: { label: string, page: string };
    projects: { label: string, page: string };
  };
  